1. Rename mod, find and replace all in folder
- `esctemplate` => newname
- `ESCTEMPLATE` => NEWNAME
- Change `esctemplate.lua` to newname.lua
- /exported 
    - replace all ~esctemplate.png to newname.png
    - delete .tex and .xml files, tools will rebuild these
- /anim 
    - delete all
    
2. 